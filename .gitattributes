# ----------------------------------------
# ✅ 改行コードの統一設定
# ----------------------------------------

# すべてのテキストファイルをLF（UNIX改行）に統一
* text=auto eol=lf

# Windows環境でも警告を出さず安全に変換
*.js      text eol=lf
*.jsx     text eol=lf
*.ts      text eol=lf
*.tsx     text eol=lf
*.css     text eol=lf
*.scss    text eol=lf
*.html    text eol=lf
*.json    text eol=lf
*.md      text eol=lf
*.yml     text eol=lf
*.yaml    text eol=lf
*.env     text eol=lf
*.sh      text eol=lf

# ----------------------------------------
# 🚫 バイナリファイルの扱い
# ----------------------------------------

# 画像・音声・動画・フォントなどはGitに変換させない
*.png     binary
*.jpg     binary
*.jpeg    binary
*.gif     binary
*.svg     binary
*.ico     binary
*.mp3     binary
*.wav     binary
*.mp4     binary
*.ttf     binary
*.woff    binary
*.woff2   binary

# ----------------------------------------
# 🧩 Vite / React 特有ファイル
# ----------------------------------------

# Viteキャッシュやビルド出力は通常 .gitignore で除外
# ここでは改行扱いのみ定義
vite.config.* text eol=lf
tsconfig.*    text eol=lf

# ----------------------------------------
# 💡 注意事項
# ----------------------------------------
# この設定により、GitHub上ではLFで統一されます。
# Windowsでcloneしても、自動でCRLF→LF変換が行われます。
